#include <bits/stdc++.h>
using namespace std;

string x, y;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cin >> x >> y;
    if (x == y)
        cout << x << endl;
    else
        cout << 1 << endl;
    return 0;
}
